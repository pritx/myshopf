import React, { useState } from "react";

const BrowseCategories = (props) => {
    const [category, setCategory] = useState(props.catdata);
  return (
    <div>
    </div>
  );
};

export default BrowseCategories;
